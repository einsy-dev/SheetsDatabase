{
  "type": "module",
  "scripts": {
    "dev": "npm run build && npm run publish",
    "build": "node ./build.js",
    "publish": "clasp push"
  },
  "devDependencies": {
    "@types/google-apps-script": "^2.0.8",
    "esbuild": "^0.27.2",
    "esbuild-copy-files-plugin": "^1.2.1",
    "esbuild-plugin-replace": "^1.4.0"
  },
  "dependencies": {
    "esbuild-plugin-clean": "^1.0.1"
  }
}
